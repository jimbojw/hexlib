/**
 * event.js
 * Tests for hex.event.js functionality.
 */

module("event");

test("hex.addEvent()", function() {
  expect(0);
  // TODO: Add tests!
});

test("event.inside()", function() {
  expect(0);
  // TODO: Add tests!
});

